[gd_scene load_steps=3 format=3 uid="uid://hqcjteaef1ju"]

[ext_resource type="PackedScene" uid="uid://cj7qg023wmgrg" path="res://src/slides/slide.tscn" id="1_p1nhe"]
[ext_resource type="Texture2D" uid="uid://cdwqfy1dwjk6x" path="res://assets/images/built_in_functions.png" id="2_alsxk"]

[node name="BuiltInFunctions" instance=ExtResource("1_p1nhe")]

[node name="VBoxContainer" type="VBoxContainer" parent="MarginContainer" index="0"]
layout_mode = 2
theme_override_constants/separation = 20

[node name="RichTextLabel" type="RichTextLabel" parent="MarginContainer/VBoxContainer" index="0"]
layout_mode = 2
theme_type_variation = &"SlideHeader"
text = "Built-in Functions"
fit_content = true

[node name="RichTextLabel2" type="RichTextLabel" parent="MarginContainer/VBoxContainer" index="1"]
layout_mode = 2
size_flags_vertical = 3
bbcode_enabled = true
text = "

When you attach a script to a node, it will already have a few functions defined:
   •   _ready() is a function that will be called once when the node first enters the world
   •   _process(delta) is a function that runs every frame, for code that needs to run continuously. The delta parameter is the elapsed time since the last frame

There are also a few other built-in functions that all nodes will recognize:
   •   _init() is run as soon as the node is created, called before the node enters the world
   •   _input(event) is called whenever an input action occurs
         •   _unhandled_input(event) is a variant that is only called for inputs not handled elsewhere"
metadata/_edit_lock_ = true

[node name="BuiltInFunctions" type="Sprite2D" parent="." index="2"]
position = Vector2(766, 107)
scale = Vector2(0.7224832, 0.6741878)
texture = ExtResource("2_alsxk")
